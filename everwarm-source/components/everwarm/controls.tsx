'use client';
import {Select,SelectTrigger,SelectValue,SelectContent,SelectItem} from '@/components/ui/select';
export function Choice({id,label,options,value,onChange,disabled=false,required=false,error}:{id:string;label:string;options:string[];value:string;onChange:(v:string)=>void;disabled?:boolean;required?:boolean;error?:string}){return <div className="field"><label htmlFor={id}>{label}{required&&<span> *</span>}</label><Select value={value} onValueChange={onChange} disabled={disabled} required={required}><SelectTrigger id={id} className="choice" aria-invalid={!!error} aria-describedby={error?id+'-error':undefined}><SelectValue placeholder="Select an option"/></SelectTrigger><SelectContent>{options.map(v=><SelectItem key={v} value={v}>{v}</SelectItem>)}</SelectContent></Select>{error&&<p className="error" id={id+'-error'}>{error}</p>}</div>}
export function Urgency({value}:{value:string}){return <span className={'badge urgency-'+value.toLowerCase()}>{value==='Urgent'?'! Urgent':'Standard'}</span>}
